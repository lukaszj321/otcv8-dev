---
title: TFS patch — extended opcode
---

# TFS patch — extended opcode

```{note}
Plik patcha znajduje się w repozytorium: `tools/tfs_extendedopcode.patch`.
Poniżej pełna zawartość (diff).
```

```{literalinclude} ../../tools/tfs_extendedopcode.patch
:language: diff
```

## Dataset: extended opcodes
Poniższa tabela stanowi **wejście do RAG** i dokumentuje zmapowane opcodes.

```{warning}
Missing CSV file: `../../authoring/05_network/datasets/extended_opcodes.csv`

Either add the dataset or update the directive.
```
