---
name: "vc16"
applyTo:
  - "vc16/**/*"
read:
  - "vc16/**"
  - "src/**"
write:
  - "docs/authoring/15_vc16/**"
constraints:
  - "UTF-8"
  - "LF"
  - "idempotent"

outputs:
  - "docs/authoring/15_vc16/index.md"
  - "docs/authoring/15_vc16/datasets/vc16_projects.csv"
  - "docs/authoring/15_vc16/diagrams/projects_deps.mmd"
---

# VC16 (Windows) — Instructions

## Goal
Zmapuj projekty/solucje, konfiguracje, toolset, biblioteki i zależności (Visual C++ 2019).

## Datasets
- `vc16_projects.csv` kolumny:
  - `solution,project,config,platform,toolset,runtime_lib,includes[],libs[],defines[]`
- Pola z `[]` serializuj jako **JSON arrays** w CSV (np. `["/Ipath","/Ipath2"]`).

## Diagrams (opcjonalnie)
- `diagrams/projects_deps.mmd` (flowchart „graph TD”).
- 1. linia wymagana:
```

%%{init: {'theme':'neutral','themeVariables':{'primaryTextColor':'#ddd','lineColor':'#9aa0a6'}}}%%

```

## Crosslinks
- Dodaj xref do: `01_core` (C++ źródła) i `05_network` (liby/protokół).

## Index
- `index.md`: frontmatter, `{toctree}` (hidden), `{contents} :local:`,
`{csv-table}` z `vc16_projects.csv`, `{mermaid}` (jeśli diagram).

## Notes
- Parsuj `.sln`/`.vcxproj`/`.props`:
- `toolset` z `<PlatformToolset>`, `runtime_lib` z `<RuntimeLibrary>`.
- `includes[]` z `<AdditionalIncludeDirectories>`.
- `libs[]` z `<AdditionalDependencies>`/`.props`.
- `defines[]` z `<PreprocessorDefinitions>`.
- Zbieraj per `config` (Debug/Release) i `platform` (Win32/x64).

## Acceptance
- [ ] `index.md` wygenerowany
- [ ] `vc16_projects.csv` istnieje; listy to JSON arrays
- [ ] (Jeśli diagram) Mermaid renderuje się (init w 1. linii, ASCII strzałki)
- [ ] Crosslinki do `01_core` i `05_network` istnieją
