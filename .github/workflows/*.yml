name: Concurrency placeholder

on:
  workflow_dispatch: {}   # uruchamiany ręcznie

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ok:
    runs-on: ubuntu-latest
    steps:
      - run: echo "workflow OK"
